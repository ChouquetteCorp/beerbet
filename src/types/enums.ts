export enum UserResults {
  Victory = 'victory',
  Defeat = 'defeat',
}

export enum ParticipationModes {
  PUBLIC = 'public',
  ENTERPRISE = 'enterprise',
  PRIVATE = 'private',
}

export enum SocialProviders {
  Google = 'google',
  Facebook = 'facebook',
}

export enum EventActions {
  Delete = 'delete',
  Modify = 'modify',
  Close = 'close',
}
